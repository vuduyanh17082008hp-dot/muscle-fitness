# Names and placeholders only. Copy to .env.local and fill locally.
# Never commit real values. Never copy from a hosted backup file.

# Public client config
NEXT_PUBLIC_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_YOUR_KEY
NEXT_PUBLIC_SUPABASE_ANON_KEY=YOUR_LEGACY_ANON_JWT
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Server secrets — never prefix these with NEXT_PUBLIC_
OPENAI_API_KEY=
OPENAI_MODEL=
USDA_FDC_API_KEY=
NCBI_API_KEY=

# Knowledge ingestion script only (not needed for app runtime)
# SUPABASE_SECRET_KEY=
