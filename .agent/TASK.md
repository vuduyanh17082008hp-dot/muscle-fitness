# TASK

## Closed
- Phase 1 core; Phase 2 Tasks 1–4 (durable VersionedState, live provider, robustness, independent validation).
- Task 5 — Phase 2.5a minimal adaptive persona + continual personalization (see STATE.md).
- Task 5R — adversarial repair of Phase 2.5a (R1–R6 + NB-1 + 10-obligation closure; see STATE.md).
- Task 5R2 — authority boundary closure (P-10: ResponseBlock authority, per-block degradation, classifier CI contract; see STATE.md).
- Final micro-repair — dual laterality + safety-scoped siblings (see STATE.md).

## Next actionable (needs a user decision — nothing is started)
1. Phase 2.5b (explicitly deferred): TECHNICAL / TASK_FOCUSED context modes, custom vocatives, regression corpus for fuzzy
   persona predicates. Do NOT start without an explicit go.
2. Open, unrelated to 2.5a: apply the coherence migration to a non-hosted/confirmed-dev project (needs explicit approval);
   pre-existing lint error `components/dante-chat.tsx:1014`; `realizePersona`/Phase 1 finalizer blank-line flattening.

## Standing constraints
- No commit / push / deploy. Hosted Supabase project `jlwszvtitjtgothgxubo` is off-limits until confirmed dev.
- Do not add mandatory LLM calls to normal turns. Do not weaken tests (spec-forced test changes must be flagged in the report).
